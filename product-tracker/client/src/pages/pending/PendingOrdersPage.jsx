// src/pages/PendingOrdersPage.jsx

import React from "react";

/**
 * PendingOrdersPage
 *
 * Placeholder component for now. The pending orders logic and UI
 * will be implemented later once we complete the “Received Orders” work.
 */
const PendingOrdersPage = () => {
  return (
    <div className="p-6">
      {/* TODO: Implement Pending Orders logic and UI */}
      <h2 className="text-xl font-semibold">Pending Orders</h2>
      <p className="mt-4 text-gray-600">
        This page is under construction. We’ll add the pending orders logic
        once the received‐orders flow is complete.
      </p>
    </div>
  );
};

export default PendingOrdersPage;
